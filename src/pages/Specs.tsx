import { Box, Typography } from '@mui/material';

export default function Specs() {
  return (
    <Box>
      <Typography variant="h5" sx={{ mb: 2 }}>Specs & Standards</Typography>
      <Typography variant="body2" color="text.secondary">Coming soon — standard conditions, nominal ranges, and target values for all monitored metrics.</Typography>
    </Box>
  );
}
